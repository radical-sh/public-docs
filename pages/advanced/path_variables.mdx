# Path Variables

Path variables are special variables that can be added in radical where base path or url is needed

```/api/v1/clients/{clientId}/accounts/{accountId}```

`clientId` and `accountId` will be considered as path variables

Path variables can be used in

* Base path in settings
* Path in API tab at resource level setting
* Custom API - Request Path
* Rest Requests